name: aves_utils
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.4 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  material_color_utilities:
  vector_math:

dev_dependencies:
  flutter_lints:

flutter:
