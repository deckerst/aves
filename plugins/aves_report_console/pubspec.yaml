name: aves_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  aves_report:
    path: ../aves_report

dev_dependencies:
  flutter_lints:

flutter:
