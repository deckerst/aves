name: aves_magnifier
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_utils:
    path: ../aves_utils
  equatable:
  provider:
  tuple:

dev_dependencies:
  flutter_lints:

flutter:
