name: aves_magnifier
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable:
  provider:
  tuple:

dev_dependencies:
  flutter_lints:

flutter:
