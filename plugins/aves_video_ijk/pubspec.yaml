name: aves_video_ijk
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_model:
    path: ../aves_model
  aves_video:
    path: ../aves_video
  aves_utils:
    path: ../aves_utils
  collection:
  fijkplayer:
    git:
      url: https://github.com/deckerst/fijkplayer.git
      ref: aves

dev_dependencies:
  flutter_lints:

flutter:
