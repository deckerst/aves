name: aves_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.11.4 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  aves_report:
    path: ../aves_report
  firebase_core:
  firebase_crashlytics:
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
