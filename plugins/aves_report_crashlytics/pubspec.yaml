name: aves_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_report:
    path: ../aves_report
  collection:
  firebase_core:
  firebase_crashlytics:
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
