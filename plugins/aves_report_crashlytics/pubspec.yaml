name: aves_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  aves_report:
    path: ../aves_report
  firebase_core:
  firebase_crashlytics:

dev_dependencies:
  flutter_lints:

flutter:
