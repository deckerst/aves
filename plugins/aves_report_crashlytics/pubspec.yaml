name: aves_report_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0" # incoherent dartfmt from 3.7.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  aves_report:
    path: ../aves_report
  # `firebase_core` v4.0.0 requires minSdkVersion 23
  firebase_core:
  # `firebase_crashlytics` v5.0.0 requires minSdkVersion 23
  firebase_crashlytics: ^4.3.10
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
