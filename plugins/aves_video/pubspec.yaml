name: aves_video
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_model:
    path: ../aves_model
  aves_utils:
    path: ../aves_utils

dev_dependencies:
  flutter_lints:

flutter:
