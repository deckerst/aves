name: aves_video
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  aves_model:
    path: ../aves_model
  aves_utils:
    path: ../aves_utils
  leak_tracker:

dev_dependencies:
  flutter_lints:

flutter:
