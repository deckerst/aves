name: aves_video
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  aves_model:
    path: ../aves_model

dev_dependencies:
  flutter_lints:

flutter:
