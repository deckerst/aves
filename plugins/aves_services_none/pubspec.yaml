name: aves_services_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_map:
    path: ../aves_map
  aves_services:
    path: ../aves_services
  latlong2:

dev_dependencies:
  flutter_lints:

flutter:
