name: aves_map
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  aves_ui:
    path: ../aves_ui
  collection:
  custom_rounded_rectangle_border:
  equatable:
  fluster:
  flutter_map:
  intl:
  latlong2:
  leak_tracker:
  provider:

dev_dependencies:
  flutter_lints:

flutter:
