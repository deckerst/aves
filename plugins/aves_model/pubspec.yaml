name: aves_model
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  collection:
  equatable:
  meta:

dev_dependencies:
  flutter_lints:

flutter:
