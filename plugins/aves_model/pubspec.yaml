name: aves_model
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  collection:
  equatable:
  meta:

dev_dependencies:
  flutter_lints:

flutter:
