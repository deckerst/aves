name: aves_report
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  collection:
  stack_trace:

dev_dependencies:
  flutter_lints:

flutter:
