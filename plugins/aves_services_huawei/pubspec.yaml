name: aves_services_platform
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.19.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  aves_map:
    path: ../aves_map
  aves_platform_meta:
    path: ../aves_platform_meta
  aves_services:
    path: ../aves_services
  huawei_hmsavailability:
  huawei_map:
  latlong2:
  provider:

dev_dependencies:
  flutter_lints:

flutter:
