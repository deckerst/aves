name: aves_magnifier
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable:
  provider:

dev_dependencies:
  flutter_lints:

flutter:
